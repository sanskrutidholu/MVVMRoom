<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mvvmroom">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MVVMRoom">
        <activity
            android:name=".sample_2.MainActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".sample_1.UpdateActivity"
            android:exported="false" />
        <activity
            android:name=".sample_1.AnotherActivity"
            android:exported="false" />
        <activity
            android:name=".sample_1.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>